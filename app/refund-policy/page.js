import LegalPage from '@/components/LegalPage'

export default function RefundPolicyPage() {
  return (
    <LegalPage title="Refund Policy">
      <p>Subscription, usage, and professional-service refund rules should be configured to match your billing provider and customer contract.</p>
      <p>This starter includes Stripe-ready structure but does not charge customers until production keys are added.</p>
    </LegalPage>
  )
}
